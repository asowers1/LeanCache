import Quick
import Nimble
import LeanCache

class RGBTests: QuickSpec {
    override func spec() {
        describe("LeanCache") {
            it("works") {
                expect(true).to(beTrue())
            }
        }
    }
}
